options(repos = c(CRAN = "https://cloud.r-project.org"))
source("renv/activate.R")
options(statnipokladna.dest_dir = "data-input/sp_data")

Sys.setenv(R_CONFIG_ACTIVE = "default")
Sys.setenv(R_CONFIG_FILE = "config.yml")
